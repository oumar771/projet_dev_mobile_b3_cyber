name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.0.0
  geolocator: ^11.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  flutter_riverpod: ^2.5.1
  state_notifier: ^1.0.0
  riverpod_annotation: ^2.3.5
  web_socket_channel: ^3.0.3
  url_launcher: ^6.2.6


  path_provider: ^2.0.11
  connectivity_plus: ^6.0.3
  google_sign_in: ^6.2.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8
  riverpod_generator: ^2.4.0

flutter:
  uses-material-design: true

  # Déclaration des assets (images, fichiers, etc.)
  assets:
    - assets/